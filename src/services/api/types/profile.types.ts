export interface Profile {
  id: string;
  name: string;
  email: string;
  // Agrega más campos según necesites
}

export interface ProfileUpdateData {
  name?: string;
  email?: string;
  // Agrega más campos según necesites
} 